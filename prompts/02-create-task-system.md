# Prompt 2 — create durable task/backlog/autonomy state

Read this repository first, especially:
- `docs/pipeline.md`
- `docs/automation-architecture.md`
- `templates/`

Then in the target repo:
- create the durable task directory contract
- create the backlog directory contract
- create the automation state root
- add initial autonomy/config/ledger stubs
- create one sample task/bootstrap artifact set

Do not create cron workers yet.
Verify all created files exist before finishing.
